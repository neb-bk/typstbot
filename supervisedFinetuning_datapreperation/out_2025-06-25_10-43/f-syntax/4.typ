
    `print(1)`
    ```
-   **Code expression:** Use the `#` prefix followed by the code. For example, `#rect(width: 1cm)`.  Note that in code blocks, you don't need to use `#` prefix for new expressions.
    ```typ
    #rect(width: 1cm)
    