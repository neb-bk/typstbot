
#align(center, block("This text block is centered."))

#align(right, block(
  [This paragraph is right-aligned.
  It can contain multiple lines of text.]
))

#align(center, block(
  #image("../../../image.png", width: 50%)
))

#align(left, block(
  #text(red, "This is a left-aligned red text.")
))
