
// Even if there's a global setting for center or right alignment, this will be left.
#align(left, block($ x = 5 $))

#text(10pt, "This specific equation is left-aligned.")

$ y = 2x + 1 $ // This equation will follow the document's default or global alignment.
