
// Without italic, 'mass' and 'velocity' would be treated as commands.
// With italic, they become multi-letter variables.

$ (italic("mass") * italic("velocity")^2) / 2 $

// A more conventional way for kinetic energy:
$ E_k = (1/2) italic("m") italic("v")^2 $
