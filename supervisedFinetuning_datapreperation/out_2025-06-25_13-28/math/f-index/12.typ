
// Without proper spacing, this might look bad:
The value is $display(A^2 + B^2) / display(C^2 - D^2)$, which is significant.

// With spacing using a box:
The value is #box($display(A^2 + B^2) / display(C^2 - D^2)$, inset: 0.15em), which is significant. This provides a clean separation.
